```json
{
  "statement": "#### **Problem Description**\nIn deep learning models, activation functions introduce non-linearity, enabling the network to learn complex patterns. You are tasked with implementing several common activation functions and their derivatives. Specifically, you will implement the Sigmoid, ReLU, and Leaky ReLU functions, along with their respective gradients.\n\nLet $x$ be the input to an activation function $f(x)$. The output is $y = f(x)$. The gradient of the loss with respect to the input $x$ is $\\frac{\\partial \\mathcal{L}}{\\partial x} = \\frac{\\partial \\mathcal{L}}{\\partial y} \\cdot \\frac{\\partial y}{\\partial x}$. Your task is to compute $\\frac{\\partial y}{\\partial x}$ for each function.\n\n#### **Input Specification**\nInput is read from `input.json` containing:\n- `inputs`: A list of $N$ floating-point numbers, representing the input values to the activation functions. Constraints: $-1000 \\le x_i \\le 1000$ for all $i$, $1 \\le N \\le 10000$.\n- `activation_type`: A string, either 'sigmoid', 'relu', or 'leaky_relu'.\n- `leaky_relu_alpha`: A floating-point number, the negative slope coefficient for Leaky ReLU. Constraints: $0 < \\alpha < 1$.\n\n#### **Output Specification**\nOutput must be written to `output.json` as a dictionary containing:\n- `outputs`: A list of $N$ floating-point numbers, the result of applying the specified activation function to each input.\n- `gradients`: A list of $N$ floating-point numbers, the derivative of the activation function with respect to its input for each input value.\n\nPrecision tolerance for floating-point comparisons: $10^{-6}$.\n\n#### **Example**\n**Input:**\n```json\n{\n  \"inputs\": [1.0, -1.0, 0.0],\n  \"activation_type\": \"sigmoid\",\n  \"leaky_relu_alpha\": 0.01\n}\n```\n**Output:**\n```json\n{\n  \"outputs\": [0.7310585786300049, 0.2689414213699951, 0.5],\n  \"gradients\": [0.19661193324148185, 0.19661193324148185, 0.25]\n}\n```\n\n#### **Note**\nFor the example input with `activation_type: \"sigmoid\"`:\n\n1.  **Input values:** $x_1 = 1.0, x_2 = -1.0, x_3 = 0.0$\n2.  **Sigmoid function:** $f(x) = \\frac{1}{1 + e^{-x}}$\n3.  **Derivative of Sigmoid:** $f'(x) = f(x) \\cdot (1 - f(x))$\n\n*   For $x_1 = 1.0$:\n    $f(1.0) = \\frac{1}{1 + e^{-1.0}} \\approx 0.7310585786$\\n    $f'(1.0) = 0.7310585786 \\cdot (1 - 0.7310585786) \\approx 0.1966119332$\n\n*   For $x_2 = -1.0$:\n    $f(-1.0) = \\frac{1}{1 + e^{-(-1.0)}} = \\frac{1}{1 + e^{1.0}} \\approx 0.2689414214$\\n    $f'(-1.0) = 0.2689414214 \\cdot (1 - 0.2689414214) \\approx 0.1966119332$\n\n*   For $x_3 = 0.0$:\n    $f(0.0) = \\frac{1}{1 + e^{0.0}} = \\frac{1}{1 + 1} = 0.5$\\n    $f'(0.0) = 0.5 \\cdot (1 - 0.5) = 0.5 \\cdot 0.5 = 0.25$\n\nThese computed values match the example output.",
  "theory": "#### **Theoretical Background: Activation Functions**\nActivation functions are fundamental components of artificial neural networks. They introduce non-linear transformations to the output of a neuron, allowing the network to learn complex relationships in data that linear models cannot capture.\n\n##### **1. Sigmoid Function**\nThe sigmoid function, also known as the logistic function, is defined as:\n$$ \\sigma(x) = \\frac{1}{1 + e^{-x}} $$\nIt squashes the input values into a range between 0 and 1. This makes it useful for binary classification tasks where the output can be interpreted as a probability.\n\n##### **The Gradient Vanishing Limitation**\nA significant limitation of the sigmoid function is the **vanishing gradient problem**. When the input $x$ becomes very large (positive or negative), the gradient of the sigmoid function approaches zero. Mathematically, the derivative of the sigmoid function is:\n$$ \\sigma'(x) = \\sigma(x) (1 - \\sigma(x)) $$\nAs $x \\to \\infty$, $\\sigma(x) \\to 1$, so $\\sigma'(x) \\to 1(1-1) = 0$. As $x \\to -\\infty$, $\\sigma(x) \\to 0$, so $\\sigma'(x) \\to 0(1-0) = 0$. This small gradient can cause the weights in earlier layers of a deep network to update very slowly or not at all during backpropagation, hindering learning.\n\n##### **2. Rectified Linear Unit (ReLU)**\nThe ReLU function is a piecewise linear activation function defined as:\n$$ \\text{ReLU}(x) = \\max(0, x) $$\nIt outputs the input directly if it is positive, and zero otherwise. ReLU is computationally efficient and has been shown to mitigate the vanishing gradient problem for positive inputs.\n\n##### **Derivative of ReLU:**\n$$ \\frac{d}{dx}\\text{ReLU}(x) = \\begin{cases} 1 & \\text{if } x > 0 \\\\ 0 & \\text{if } x \\le 0 \\end{cases} $$\nAt $x=0$, the derivative is technically undefined. However, in practice, it is usually set to 0 or 1. For gradient-based optimization, setting it to 0 is common, or a subgradient can be used.\n\n##### **The Dying ReLU Problem:**\nWhile ReLU addresses vanishing gradients for positive inputs, it suffers from the **dying ReLU problem**. If a neuron's input is consistently negative, it will always output 0, and its gradient will always be 0. This means the weights associated with that neuron will never be updated, effectively rendering the neuron 'dead' and useless.\n\n##### **3. Leaky ReLU**\nLeaky ReLU is a modification of ReLU designed to address the dying ReLU problem. It introduces a small, non-zero slope for negative inputs:\n$$ \\text{Leaky ReLU}(x; \\alpha) = \\max(\\alpha x, x) $$\nwhere $\\alpha$ is a small positive constant (e.g., 0.01).\n\n##### **Derivative of Leaky ReLU:**\n$$ \\frac{d}{dx}\\text{Leaky ReLU}(x; \\alpha) = \\begin{cases} 1 & \\text{if } x > 0 \\\\ \\alpha & \\text{if } x \\le 0 \\end{cases} $$\nFor $x > 0$, the derivative is 1. For $x \\le 0$, the derivative is $\\alpha$. This ensures that even for negative inputs, there is a small, non-zero gradient, preventing neurons from becoming completely inactive.\n\n##### **Properties of Activation Functions:**\n*   **Non-linearity:** Essential for learning complex patterns.\n*   **Differentiability:** Required for gradient-based optimization (backpropagation).\n*   **Range:** Some functions have bounded ranges (e.g., Sigmoid), while others are unbounded (e.g., ReLU for positive inputs).\n*   **Computational Efficiency:** Simpler functions are faster to compute.",
  "tutorial": "#### **Conceptual Idea**\nNeural networks learn by adjusting weights based on the error in their predictions. This adjustment is done using gradient descent, which requires the activation functions to be differentiable. We need to implement the forward pass (computing the output) and the backward pass (computing the gradient of the output with respect to the input).\n\n##### **1. The Problem: Implementing Activation Functions and their Gradients**\nWe need to implement three activation functions: Sigmoid, ReLU, and Leaky ReLU, and for each, compute both the output and its gradient with respect to the input. This is crucial for backpropagation in neural networks.\n\n##### **2. The Solution: Element-wise Operations and Conditional Logic**\nThese activation functions operate element-wise on the input tensor. We can leverage NumPy or PyTorch's vectorized operations for efficiency. Conditional logic will be needed for ReLU and Leaky ReLU to handle different cases based on the input's sign.\n\n##### **Sigmoid Implementation (Vectorized)**\n*   **Function:** $f(x) = \\frac{1}{1 + e^{-x}}$\n*   **Gradient:** $f'(x) = f(x) (1 - f(x))$\n\nLet `inputs` be a tensor of shape `(N,)`.\n\n1.  Compute `outputs = 1 / (1 + np.exp(-inputs))`.\n    *   `Shape: (N,)`\n2.  Compute `gradients = outputs * (1 - outputs)`.\n    *   `Shape: (N,)`\n\n##### **ReLU Implementation (Vectorized)**\n*   **Function:** $\\text{ReLU}(x) = \\max(0, x)$\n*   **Gradient:** $\\frac{d}{dx}\\text{ReLU}(x) = 1$ if $x > 0$, else $0$.\n\nLet `inputs` be a tensor of shape `(N,)`.\n\n1.  Compute `outputs = np.maximum(0, inputs)`.\n    *   `Shape: (N,)`\n2.  Compute `gradients = (inputs > 0).astype(inputs.dtype)`.\n    *   `Shape: (N,)`\n\n##### **Leaky ReLU Implementation (Vectorized)**\n*   **Function:** $\\text{Leaky ReLU}(x; \\alpha) = \\max(\\alpha x, x)$\n*   **Gradient:** $\\frac{d}{dx}\\text{Leaky ReLU}(x; \\alpha) = 1$ if $x > 0$, else $\\alpha$.\n\nLet `inputs` be a tensor of shape `(N,)` and `alpha` be a scalar.\n\n1.  Compute `outputs = np.where(inputs > 0, inputs, alpha * inputs)`.\n    *   `Shape: (N,)`\n2.  Compute `gradients = np.where(inputs > 0, 1.0, alpha)`.\n    *   `Shape: (N,)`\n\n```text\nInput Tensor (N,)\n  |           |           |\n  v           v           v\n[ x1,       x2,       x3, ... ]\n  |           |           |\n  v           v           v\nSigmoid/ReLU/Leaky ReLU (Element-wise operation)\n  |           |           |\n  v           v           v\n[ y1,       y2,       y3, ... ]  (Outputs)\n  |\n  v\nGradient Calculation (Element-wise operation based on input sign)\n  |\n  v\n[ dy1/dx1, dy2/dx2, dy3/dx3, ... ] (Gradients)\n```",
  "solution": "import json\nimport numpy as np\n\ndef sigmoid(x):\n    \"\"\"Computes the sigmoid function.\"\"\"\n    return 1 / (1 + np.exp(-x))\n\ndef sigmoid_derivative(x):\n    \"\"\"Computes the derivative of the sigmoid function.\"\"\"\n    s = sigmoid(x)\n    return s * (1 - s)\n\ndef relu(x):\n    \"\"\"Computes the ReLU activation function.\"\"\"\n    return np.maximum(0, x)\n\ndef relu_derivative(x):\n    \"\"\"Computes the derivative of the ReLU function.\"\"\"\n    return (x > 0).astype(x.dtype)\n\ndef leaky_relu(x, alpha):\n    \"\"\"Computes the Leaky ReLU activation function.\"\"\"\n    return np.where(x > 0, x, alpha * x)\n\ndef leaky_relu_derivative(x, alpha):\n    \"\"\"Computes the derivative of the Leaky ReLU function.\"\"\"\n    return np.where(x > 0, 1.0, alpha)\n\ndef implement_activation_functions(inputs, activation_type, leaky_relu_alpha):\n    \"\"\"\n    Implements specified activation functions and their derivatives.\n\n    Args:\n        inputs (np.ndarray): Input values.\n        activation_type (str): Type of activation function ('sigmoid', 'relu', 'leaky_relu').\n        leaky_relu_alpha (float): Slope for Leaky ReLU.\n\n    Returns:\n        tuple: A tuple containing (outputs, gradients).\n    \"\"\"\n    if activation_type == 'sigmoid':\n        outputs = sigmoid(inputs)\n        gradients = sigmoid_derivative(inputs)\n    elif activation_type == 'relu':\n        outputs = relu(inputs)\n        gradients = relu_derivative(inputs)\n    elif activation_type == 'leaky_relu':\n        outputs = leaky_relu(inputs, leaky_relu_alpha)\n        gradients = leaky_relu_derivative(inputs, leaky_relu_alpha)\n    else:\n        raise ValueError(f\"Unknown activation type: {activation_type}\")\n    \n    return outputs, gradients\n\nif __name__ == \"__main__\":\n    with open(\"input.json\", \"r\") as f:\n        input_data = json.load(f)\n\n    inputs = np.array(input_data.get(\"inputs\", []))\n    activation_type = input_data.get(\"activation_type\", \"relu\")\n    leaky_relu_alpha = input_data.get(\"leaky_relu_alpha\", 0.01)\n\n    outputs, gradients = implement_activation_functions(inputs, activation_type, leaky_relu_alpha)\n\n    result = {\n        \"outputs\": outputs.tolist(),\n        \"gradients\": gradients.tolist()\n    }\n\n    with open(\"output.json\", \"w\") as f:\n        json.dump(result, f, indent=2)\n",
  "coding": "import json\nimport numpy as np\nfrom typing import List, Dict, Any\n\ndef implement_activation_functions(inputs: np.ndarray, activation_type: str, leaky_relu_alpha: float) -> tuple[np.ndarray, np.ndarray]: \n    \"\"\"\n    Implements specified activation functions and their derivatives.\n\n    Args:\n        inputs (np.ndarray): Input values. Shape: (N,)\n        activation_type (str): Type of activation function ('sigmoid', 'relu', 'leaky_relu').\n        leaky_relu_alpha (float): Slope for Leaky ReLU.\n\n    Returns:\n        tuple[np.ndarray, np.ndarray]: A tuple containing (outputs, gradients).\n                                         Shape: (N,) for both.\n    \"\"\"\n    pass\n\nif __name__ == \"__main__\":\n    with open(\"input.json\", \"r\") as f:\n        input_data = json.load(f)\n\n    inputs = np.array(input_data.get(\"inputs\", []))\n    activation_type = input_data.get(\"activation_type\", \"relu\")\n    leaky_relu_alpha = input_data.get(\"leaky_relu_alpha\", 0.01)\n\n    outputs, gradients = implement_activation_functions(inputs, activation_type, leaky_relu_alpha)\n\n    result = {\n        \"outputs\": outputs.tolist(),\n        \"gradients\": gradients.tolist()\n    }\n\n    with open(\"output.json\", \"w\") as f:\n        json.dump(result, f, indent=2)\n",
  "checker": "import json\nimport numpy as np\n\ndef sigmoid(x):\n    return 1 / (1 + np.exp(-x))\n\ndef sigmoid_derivative(x):\n    s = sigmoid(x)\n    return s * (1 - s)\n\ndef relu(x):\n    return np.maximum(0, x)\n\ndef relu_derivative(x):\n    return (x > 0).astype(x.dtype)\n\ndef leaky_relu(x, alpha):\n    return np.where(x > 0, x, alpha * x)\n\ndef leaky_relu_derivative(x, alpha):\n    return np.where(x > 0, 1.0, alpha)\n\ndef check_output(candidate_output_data, expected_output_data, tolerance=1e-6):\n    candidate_outputs = np.array(candidate_output_data['outputs'])\n    candidate_gradients = np.array(candidate_output_data['gradients'])\n    expected_outputs = np.array(expected_output_data['outputs'])\n    expected_gradients = np.array(expected_output_data['gradients'])\n\n    if not np.allclose(candidate_outputs, expected_outputs, atol=tolerance):\n        raise AssertionError(f\"Outputs mismatch. Expected: {expected_outputs}, Got: {candidate_outputs}\")\n\n    if not np.allclose(candidate_gradients, expected_gradients, atol=tolerance):\n        raise AssertionError(f\"Gradients mismatch. Expected: {expected_gradients}, Got: {candidate_gradients}\")\n\n    print(\"Checker passed!\")\n\nif __name__ == '__main__':\n    with open('output.json', 'r') as f:\n        candidate_output = json.load(f)\n\n    # For testing, we'll manually define expected outputs for a few cases\n    # In a real scenario, this would be generated by a trusted reference implementation\n    # For simplicity, let's re-run the logic to get expected values for a specific test case\n    # This is a placeholder; actual checker would compare against pre-computed correct answers\n    \n    # Example: Re-calculate expected for a specific test case (e.g., the first one)\n    with open('input.json', 'r') as f:\n        input_data = json.load(f)\n    \n    inputs = np.array(input_data.get(\"inputs\", []))\n    activation_type = input_data.get(\"activation_type\", \"relu\")\n    leaky_relu_alpha = input_data.get(\"leaky_relu_alpha\", 0.01)\n\n    if activation_type == 'sigmoid':\n        expected_outputs = sigmoid(inputs)\n        expected_gradients = sigmoid_derivative(inputs)\n    elif activation_type == 'relu':\n        expected_outputs = relu(inputs)\n        expected_gradients = relu_derivative(inputs)\n    elif activation_type == 'leaky_relu':\n        expected_outputs = leaky_relu(inputs, leaky_relu_alpha)\n        expected_gradients = leaky_relu_derivative(inputs, leaky_relu_alpha)\n    else:\n        raise ValueError(f\"Unknown activation type: {activation_type}\")\n\n    expected_output = {\n        \"outputs\": expected_outputs.tolist(),\n        \"gradients\": expected_gradients.tolist()\n    }\n\n    check_output(candidate_output, expected_output)\n",
  "test_inputs": "[{\"inputs\": [1.0, -1.0, 0.0], \"activation_type\": \"sigmoid\", \"leaky_relu_alpha\": 0.01}, {\"inputs\": [2.5, -0.5, 0.0, 10.0, -10.0], \"activation_type\": \"relu\", \"leaky_relu_alpha\": 0.01}, {\"inputs\": [3.0, -2.0, 0.5, -0.1, 1.0], \"activation_type\": \"leaky_relu\", \"leaky_relu_alpha\": 0.05}, {\"inputs\": [0.0, 0.0, 0.0], \"activation_type\": \"sigmoid\", \"leaky_relu_alpha\": 0.01}, {\"inputs\": [-100.0, 100.0, 50.0, -50.0], \"activation_type\": \"leaky_relu\", \"leaky_relu_alpha\": 0.1}]"
}
```
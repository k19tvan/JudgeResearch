<!-- learning_path/problem_06/question.md -->
# Problem 06 Questions

## Multiple Choice
1. Why does the decoder need self-attention on the object queries before cross-attention?
A. Because the encoder demands it
B. To allow object query representations to interact and communicate, thereby preventing multiple queries from predicting the same object
C. To reduce dimensionality
D. To normalize the values

2. True or False: The `tgt` tensor in the very first decoder layer is initialized entirely as zeros.
A. True
B. False

## Answer Key
1.B 2.A

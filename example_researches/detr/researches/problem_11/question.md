<!-- learning_path/problem_11/question.md -->
# Problem 11 Questions

## Multiple Choice
1. Why is `loss.backward()` essential?
A. To reset the network weights
B. To compute the derivative of the combined loss function with respect to every learnable parameter using the chain rule
C. To reduce batch sizes actively
D. It normalizes inputs for the next epoch

2. In DETR's architecture, which piece typically requires the most complex hyperparameter tuning due to being End-to-End?
A. The number of positional embeddings
B. Setting the precise relative coefficients ($\lambda$) to weight L1 vs GIoU vs Classification losses effectively
C. The DropOut rate
D. The random seed

## Answer Key
1.B 2.B

<!-- learning_path/problem_10/question.md -->
# Problem 10 Questions

## Multiple Choice
1. How does the Cross Entropy loss handle unmatched object queries?
A. Unmatched queries are ignored completely
B. Unmatched queries are heavily penalized as False Positives
C. They are treated as predicting the "empty" or "background" class
D. They scale down the bounding box error dynamically

2. What does `weight_dict` help achieve?
A. Multi-gpu synchronization
B. Controlling the relative scales of CE, L1, and GIoU loss components
C. Adjusts learning rates per layer
D. Masking out specific image regions

## Answer Key
1.C 2.B

<!-- learning_path/problem_07/question.md -->
# Problem 07 Questions

## Multiple Choice
1. How does the Bounding Box prediction MLP differ from the Classification header?
A. Box MLP uses a 3-layer architecture with ReLU and Sigmoid standard bounding bounds
B. Classification doesn't use the Decoder features
C. Box MLP outputs integer values
D. They are completely identical 

2. How are the `query_embeddings` passed into the system?
A. Added directly to the image pixels
B. Handled securely by the transformer decoder mechanism as an explicit variable
C. Fetched dynamically based on object shapes
D. Processed by a ConvNet 

## Answer Key
1.A 2.B
